class Shape:
    class Square:
        def __init__(self, length):
            self.length = length

        def area(self):
            shapes_area = self.length * self.length
            print(shapes_area)
    
    def area(self):
        shapes_area = 0
        print(shapes_area)